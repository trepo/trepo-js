{
  "name": "trepo",
  "version": "0.0.0",
  "description": "",
  "main": "dist/Trepo.js",
  "scripts": {
    "build": "babel src --out-dir dist --ignore *.test.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "babel src --out-dir dist --ignore *.test.js -w"
  },
  "author": "John Clark <socrates37@gmail.com> (jonnymbgood)",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-plugin-transform-async-to-generator": "^6.8.0"
  },
  "dependencies": {
    "apollo-server": "^0.2.8",
    "graphql": "^0.7.0",
    "graphql-tools": "^0.7.2",
    "trepo-vgraph": "^0.0.0"
  }
}

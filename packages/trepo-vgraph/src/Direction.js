/**
 * The direction of an edge.
 *
 * Node --OUT--> Edge --IN--> Node
 *
 * Possible Values:
 * * IN
 * * OUT
 * * BOTH
 *
 * @typedef {String} Direction
 */
let Direction = {
  IN: 'IN',
  OUT: 'OUT',
  BOTH: 'BOTH',
};

module.exports = Direction;
